<Window x:Class="ViewLayer.ProductListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ViewLayer"
        xmlns:logic="clr-namespace:ViewModelLayer;assembly=LogicLayer"
        mc:Ignorable="d"
        Title="ProductListView" Height="450" Width="450">
    <Window.DataContext>
        <logic:ProductListViewModel/>
    </Window.DataContext>
    
    <Grid Margin="3,5,3,3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="productListView" ItemsSource="{Binding Products}" SelectedItem="{Binding Product}" Grid.Column="0" MouseDoubleClick="ProductList_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding ProductID}"  Width="80"/>
                    <GridViewColumn Header="Product Name" DisplayMemberBinding="{Binding Name}" Width="250"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Orientation = "Vertical" Grid.Column="1" HorizontalAlignment="Stretch" Margin="5,-5,0,5">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0,5,0,0" />
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="Add" Command="{Binding DisplayAddWindow}"/>
            <Button Content="Remove" Command="{Binding RemoveEntity}"/>
            <Button Content="Update"/>
            <Button Content="Info" Command="{Binding DisplayMessage}"/>
        </StackPanel>
    </Grid>
</Window>
